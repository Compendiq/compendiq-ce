# Cache headers for draw.io static files — these files lack content hashes
# in their filenames, so we use a short cache with revalidation instead of
# immutable headers to avoid serving stale files after version upgrades.
expires 7d;
add_header Cache-Control "public, no-cache" always;
