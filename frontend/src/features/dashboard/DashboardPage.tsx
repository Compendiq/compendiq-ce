/**
 * @deprecated DashboardPage has been merged into PagesPage (issue #109).
 * This file is kept temporarily for reference but is no longer routed.
 * The stats cards are now shown inline on the PagesPage.
 */
export function DashboardPage() {
  return null;
}
